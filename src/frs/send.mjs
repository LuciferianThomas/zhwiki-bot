import { Mwn } from 'mwn';
import moment from 'moment';

import { time, capitalize, log } from '../fn.mjs';
import { CDB, hash } from '../db.mjs'

// const rfcData = new CDB( 'FRS' )
// if ( !rfcData.get( 'active' ) ) rfcData.set( 'active', [] );


export default async ( bot ) => {
}